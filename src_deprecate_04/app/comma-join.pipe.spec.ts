import { CommaJoinPipe } from './comma-join.pipe';

describe('CommaJoinPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaJoinPipe();
    expect(pipe).toBeTruthy();
  });
});
