import { BackListIdPipe } from './back-list-id.pipe';

describe('BackListIdPipe', () => {
  it('create an instance', () => {
    const pipe = new BackListIdPipe();
    expect(pipe).toBeTruthy();
  });
});
