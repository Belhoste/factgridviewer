import { SlashJoinPipe } from './slash-join.pipe';

describe('SlashJoinPipe', () => {
  it('create an instance', () => {
    const pipe = new SlashJoinPipe();
    expect(pipe).toBeTruthy();
  });
});
